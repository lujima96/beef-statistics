Goal is to create an ingestion tool separate from the beef statistics app that abstracts hard pulling the data
The goal of the tool is to be able to place a link, and a file type.It will
then be able to retrieve that data through selenium headless browser scraper. This app should also allow us to handle
updating the database, deleting tables, creating tables. We are separating responsibilities.