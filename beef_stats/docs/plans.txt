Use OpenStreetMap + OSRM / GrassHopper-style engine.

Integration plan -->

3. Keep persistence symmetrical
Update the applyPersistedState helper so it hydrates your new fields and guards against missing data, just as it already does for the current state slices. On the server, consider validating the incoming payload before writing it to disk (e.g., add a Pydantic model around _write_state_file) so malformed coordinates don’t corrupt future loads.

4. Add a routing service endpoint
Create a new FastAPI route (for example, /delivery-estimator/route-plan) that accepts ordered stops and calls whichever routing engine you choose (OSRM, GraphHopper, Valhalla, etc.). Keep using buildApiUrl on the frontend so the SPA can fetch both the state and the new planning endpoint without hard-coding origins. This is a good place to add caching and error handling that would be awkward to manage in the browser.

5. Surface planning controls in the UI
Once the backend returns leg-by-leg distances and durations, feed those values into the existing “Auto” buttons in the truck cost table—those controls are already rendered but disabled today. You can pre-fill TripLogEntry.milesDriven with the routed mileage, compute fuel/maintenance totals, and split the vendor list across trucks by assigning contiguous stop ranges or solving a VRP before saving the updated state.

6. Iterate toward optimization
With coordinates and travel times in place, start with a simple heuristic: partition the ordered stop list so that each truck gets roughly equal drive time and service minutes, then refine into a full VRP solver as needed. Persist the per-truck stop assignments alongside the existing trucks array so the worker-assignment view can show each truck’s workload without major UI surgery.